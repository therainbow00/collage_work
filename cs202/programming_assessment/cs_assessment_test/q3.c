/*
Write a loop that prints the contents of the linked list starting at head, one number per line. Use the variables
declared below.
typedef struct node {
    int number;
    struct node *link;
} NODE;
int main()
{
    NODE *head, *ptr;
}
*/
/*
Assume that the list is somehow created here.
*/

#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <string.h>

typedef struct node
{
    int number;
    struct node *link;
} NODE;
int main()
{
    NODE *head, *ptr;

}
