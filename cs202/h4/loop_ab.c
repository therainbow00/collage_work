#include <stdio.h>

int main(int argc, char *argv[])
{
    int i, j, k;
    char a, b;
    scanf("%d", &k);
    //printf("1\n");
    //printf("(1)\n");
    for (i = 0; j < k; j++)
    {
        putchar('a');
    }

    for (k = i; k <= k; k++)
    {
        putchar('b');
        //printf("\n(j): %d, (i): %d", j, i);
    }
    printf("\n");
    return 0;
}
